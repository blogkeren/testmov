name: Keep Render Alive



on:

  schedule:

    # Jalan setiap 14 menit (agar tidak kena sleep time 15 menit)

    - cron: '*/14 * * * *'

  # Agar bisa dijalankan manual dari tab Actions di GitHub

  workflow_dispatch:



jobs:

  ping:

    runs-on: ubuntu-latest

    steps:

      - name: Ping Render App

        # Ganti dengan URL Render Anda

        run: curl -I https://testmov.onrender.com
